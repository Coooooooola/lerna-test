{
  "name": "lerna-test",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "lerna run clean --stream --parallel",
    "build": "lerna run --stream build"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2",
    "typescript": "^3.8.3",
    "@babel/cli": "^7.8.4",
    "@babel/core": "^7.9.0"
  }
}
